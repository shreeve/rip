export default
  name: 'hello'
  hosts: ['hello.ripdev.io']
  app:
    root: 'app'             # default
    manifest:               # default
      update: ['**/*.rip']  # default
      css: ['**/*.css']     # default
      reload: ['**/*.html'] # default
