export default
  name: 'hello'
  hosts: ['hello.ripdev.io'] # the edge owns standard HTTPS on port 443
  app:
    root: 'app'             # default
    manifest:               # default
      update: ['**/*.rip']  # default
      css: ['**/*.css']     # default
      reload: ['**/*.html'] # default
