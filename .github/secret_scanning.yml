# Intentionally committed development-only private key for the publicly
# trusted ripdev.io certificate. The zone resolves only to loopback; see
# packages/site/Caddyfile.
paths-ignore:
  - "packages/site/ripdev.io.key"
