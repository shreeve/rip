# Simple Rip test program
name = "Rip"
console.log "Hello from #{name}!"

# Test some Rip features
square = (x) -> x * x
result = square 5

console.log "5 squared is #{result}"

# Test object literal
person =
  name: "Steve"
  age: 42
  greet: -> console.log "Hi, I'm #{@name}"

person.greet()
