# Simple test server
import { Hono } from 'hono'

app = new Hono()

app.get '/ping', (c) -> c.text 'pong from rip!'
app.get '/config', (c) -> c.json { message: 'Config from rip!' }

console.log 'App created, type:', typeof app
console.log 'App has fetch:', typeof app.fetch

export default app