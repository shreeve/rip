export default
  name: 'pulse'
  hosts: ['pulse.via.rip', 'pulse.local'] # loopback + LAN (reachable per `janus mode`)
  hub:
    bridge: 'hub'           # Janus Bam door for live App watch + /pulse collab
  app:
    root: 'app'
