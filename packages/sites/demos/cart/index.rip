import { hub, start } from 'rip/sites'
import { setup } from './setup.rip'
import './api/index.rip'

setup!

# Required: an API-entry site registers /hub regardless, and Janus refuses the
# live-watch socket if this POST 404s. No accounts here, so the door is open.
hub auth: false

start()
