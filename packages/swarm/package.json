{
  "name": "@rip-lang/swarm",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "description": "Parallel job runner with worker threads — setup once, swarm many",
  "exports": {
    ".": "./swarm.rip"
  },
  "bin": {
    "swarm": "./swarm.rip"
  },
  "scripts": {
    "test": "rip test.rip"
  },
  "files": [
    "swarm.rip",
    "README.md"
  ],
  "devDependencies": {
    "@rip-lang/testing": "workspace:*"
  }
}
