# A script that names its own queue: state lives under .swarm/alpha,
# never in the shared .swarm.

import { swarm, init, retry, todo } from 'rip/swarm'

setup = ->
  unless retry()
    init()
    todo i for i in [1..3]

perform = (task) -> null

swarm { setup, perform, tag: 'alpha' }
