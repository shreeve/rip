export type Entry = { href: RoutePath, name: string, description: string }

export COMPONENTS: Record<'dialog' | 'drawer', Entry> =
  dialog: { href: '/dialog', name: 'Dialog', description: 'A modal that opens on top of the entire page.' }
  drawer: { href: '/drawer', name: 'Drawer', description: 'A panel that slides in from an edge and swipes away.' }
