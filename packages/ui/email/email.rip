# ==============================================================================
# rip/ui/email — curated email rendering + public component surface
#
# Usage:
#   import { toHTML, Email, Head, Body, Container, Heading, Text, Button } from 'rip/ui/email'
#   html = toHTML(MyEmail, name: "Alice")
# ==============================================================================

import type { CSSProperties as StyleObject, StyleValue as StyleUnion } from '../shared/styles.rip'

export type CSSProperties = StyleObject
export type StyleValue = StyleUnion
export { mergeStyles, parseStyle } from '../shared/styles.rip'
export { toHTML, toText, toEmail } from './render.rip'
export {
  Email, Head, Body, Preview, Font,
  Container, Section, Row, Column, Heading, Text, Link, Image, Divider, Button,
  Markdown, CodeBlock, CodeInline, Tailwind
} from './components.rip'
