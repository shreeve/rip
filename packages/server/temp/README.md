# Rip Server burn-down

This directory holds the implementation that has not yet earned a place in the
reconstructed server. A responsibility leaves this tree only when a verification
fixture requires it, replaces it, assigns it to Janus/Caddy/application code, or
rejects it explicitly.

The directory is temporary and does not ship in the package.
