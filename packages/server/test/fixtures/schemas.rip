# Sibling packages resolve by direct path through the Rip loader.
# Importing validate registers its ~:name coercers (they ride the
# main import).
import '../../../validate/validate.rip'

export CreateOrder = schema
  total! ~:money
  placed! ~:date
  note? ~:string

export Rename = schema
  name! ~:name
