{
  "name": "@rip-lang/server",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "description": "Sinatra-style web framework — routes, smart responses, read() validation, and AsyncLocalStorage-powered request context; the same app runs standalone or under a worker pool behind Janus",
  "exports": {
    ".": "./server.rip",
    "./middleware": "./middleware.rip"
  },
  "bin": {
    "rip-server": "./server.rip"
  },
  "scripts": {
    "test": "rip test.rip"
  },
  "files": [
    "server.rip",
    "middleware.rip",
    "worker.rip",
    "manager.rip",
    "README.md"
  ],
  "dependencies": {
    "@rip-lang/validate": "workspace:*"
  },
  "devDependencies": {
    "@rip-lang/testing": "workspace:*"
  }
}
