// @rip-lang/ai — an MCP stdio server for persistent multi-model AI
// consultation (chat, discuss, panel, fresh_review, conversation
// management, content-hashed attachments, latest-model autodetect).
//
// The module is an executable, not a library: run under `import.meta.main`
// (via the `rip-ai` bin) it speaks JSON-RPC 2.0 over stdin/stdout and
// exposes eleven tools — status, list_models, chat, discuss, panel,
// fresh_review, list_conversations, get_conversation, delete_conversation,
// export_conversation, redact. Imported as a module it exports nothing and
// starts nothing.

export {};
