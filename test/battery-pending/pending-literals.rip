# Pending rows for literals — behaviors the battery does not cover yet.
# Every row here FAILS today; when a feature revives one, MOVE the row
# into test/battery/ in the same change (the lane asserts still-failing).

import { test, code, fail, type } from '../support/testing.js'

test 'inline JavaScript evaluation', '''
  `var x = 5`
  x
''', 5

test 'block inline JavaScript', '''
  ```
  let a = 1;
  let b = 2;
  ```
  c = 3
  a + b + c
''', 6

test 'JS blocks mixed with Rip', '''
  x = 1
  ```
  let y = 2;
  ```
  z = 3
  x + y + z
''', 6

test 'inline JS with return value', '''
  result = `(function() { return 42; })()`
  result
''', 42

test 'multiple JS variables', '''
  ```
  let a = 10;
  let b = 20;
  let c = 30;
  ```
  a + b + c
''', 60
