# 10-modules-lib.rip — the module the 10-family imports: named exports, a promoted local, and an indented default.

station = 'relay'

export host = 'localhost'
export port: number = 4010
export describe! = -> console.log('lib describes', host)

export { station }

export default
  theme: 'dark'
  depth: 2

console.log('lib loaded:', station)
