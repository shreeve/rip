# 10-modules-alias.rip — the aliased default: a local promoted through the specifier list, never `export default`.

palette = { accent: 'teal', tone: 'calm' }

export { palette as default }

console.log('alias set:', palette.accent)
