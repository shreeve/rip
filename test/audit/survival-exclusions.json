{
  "01-basics.rip": {
    "46:10:alpha": "symbol literal — lowers into a string"
  },
  "03-collections.rip": {
    "9:12:alpha": "lowered into a string — the face holds these bytes inside a literal",
    "9:22:beta": "lowered into a string — the face holds these bytes inside a literal"
  },
  "04-assignments.rip": {
    "75:96:length": "member read on `restScores`, a rest bound by a declaring array pattern and so tuple-typed (`[number, number]`) — tsgo issues no token for a tuple's `length`, while an array's `length` tokens `property`; probe-verified on hand-written TS",
    "95:20:alpha": "INTERIM — hoist cost, not twin-equivalent: idiomatic hand-written TS tokens this. `keyName`'s hoist widens it from 'alpha' to string, degrading `dynamic`'s computed key to an index signature — the member read loses its symbol (probe-verified: a const key tokens)"
  },
  "07-exceptions.rip": {
    "22:40:length": "member read on `message`, which hovers `any` (verified 2026-08-03) — no symbol, no token; tsgo behaves identically on hand-written TS",
    "138:46:message": "member read on `error`, bound by an annotated `catch error: any` — a member of `any` has no symbol, no token; tsgo behaves identically on hand-written TS"
  },
  "08-functions.rip": {
    "147:33:json": "INTERIM — hoist cost, not twin-equivalent: idiomatic hand-written TS tokens this. `FORMATS` is a hoisted evolving `let`; inside a closure the classifier does not flow-type the capture, so the member carries no token (probe-verified on the face's exact shape)",
    "147:51:text": "INTERIM — hoist cost, not twin-equivalent: idiomatic hand-written TS tokens this. `FORMATS` is a hoisted evolving `let`; inside a closure the classifier does not flow-type the capture, so the member carries no token (probe-verified on the face's exact shape)"
  },
  "10-modules.rip": {
    "14:24:theme": "import-binding head tsgo leaves untokenized on equivalent hand-written TS (namespace head, or a default that exports a literal) — probe-verified 2026-08-03",
    "14:37:lib": "import-binding head tsgo leaves untokenized on equivalent hand-written TS (namespace head, or a default that exports a literal) — probe-verified 2026-08-03",
    "14:71:sameTheme": "import-binding head tsgo leaves untokenized on equivalent hand-written TS (namespace head, or a default that exports a literal) — probe-verified 2026-08-03",
    "22:25:combo": "import-binding head tsgo leaves untokenized on equivalent hand-written TS (namespace head, or a default that exports a literal) — probe-verified 2026-08-03",
    "22:38:whole": "import-binding head tsgo leaves untokenized on equivalent hand-written TS (namespace head, or a default that exports a literal) — probe-verified 2026-08-03",
    "22:50:paired": "import-binding head tsgo leaves untokenized on equivalent hand-written TS (namespace head, or a default that exports a literal) — probe-verified 2026-08-03",
    "32:14:themeShape": "type-only default binding read through `typeof` — tsgo leaves it untokenized on equivalent hand-written TS (a default that exports a literal) — probe-verified 2026-08-31",
    "33:7:libShapes": "type-only namespace head in a qualified type name — tsgo leaves it untokenized on equivalent hand-written TS — probe-verified 2026-08-31",
    "54:23:hubTheme": "import-binding head tsgo leaves untokenized on equivalent hand-written TS (namespace head, or a default that exports a literal) — probe-verified 2026-08-03"
  },
  "11-types.rip": {
    "184:74:hits": "member resolves through `Counts`'s index signature — no declared property symbol, no token; probe-verified identical on hand-written TS",
    "200:14:first": "labeled tuple element — a label, not an identifier with a symbol",
    "200:29:second": "labeled tuple element — a label, not an identifier with a symbol",
    "208:62:length": "tuple `.length` — resolves through the array apparatus, no own symbol; probe-verified: hand-written TS gets no token either"
  },
  "13-components-lib.rip": {
    "4:2:offer": "gate spelling — lowers to gate scaffold, not an identifier",
    "7:4:div": "lowered into a string — the face holds these bytes inside a literal"
  },
  "13-components.rip": {
    "15:4:span": "lowered into a string — the face holds these bytes inside a literal",
    "15:33:style": "lowered into a string — the face holds these bytes inside a literal",
    "20:26:input": "lowered into a string — the face holds these bytes inside a literal",
    "26:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "28:8:label": "lowered into a string — the face holds these bytes inside a literal",
    "29:6:input": "lowered into a string — the face holds these bytes inside a literal",
    "30:8:value": "channel spelling (`<=>`) — rip-native; the lowering leaves no tokenizable identifier at this position",
    "32:8:div": "lowered into a string — the face holds these bytes inside a literal",
    "47:4:section": "lowered into a string — the face holds these bytes inside a literal",
    "48:6:h1": "lowered into a string — the face holds these bytes inside a literal",
    "49:6:span": "lowered into a string — the face holds these bytes inside a literal",
    "50:6:input": "lowered into a string — the face holds these bytes inside a literal",
    "50:12:ref": "component `ref` prop — lowers to `this`-rooted wiring; the face holds no such identifier at this position",
    "51:8:value": "channel spelling (`<=>`) — rip-native; the lowering leaves no tokenizable identifier at this position",
    "52:6:button": "lowered into a string — the face holds these bytes inside a literal",
    "52:14:click": "lowered into a string — the face holds these bytes inside a literal",
    "53:6:button": "lowered into a string — the face holds these bytes inside a literal",
    "53:14:click": "lowered into a string — the face holds these bytes inside a literal",
    "57:25:span": "lowered into a string — the face holds these bytes inside a literal",
    "58:27:span": "lowered into a string — the face holds these bytes inside a literal",
    "59:13:span": "lowered into a string — the face holds these bytes inside a literal",
    "60:6:ul": "lowered into a string — the face holds these bytes inside a literal",
    "62:10:li": "lowered into a string — the face holds these bytes inside a literal",
    "62:13:key": "render DSL attribute — lowers into a string",
    "63:6:slot": "render DSL word — lowers into a string",
    "72:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "75:8:value": "channel spelling (`<=>`) — rip-native; the lowering leaves no tokenizable identifier at this position",
    "78:8:em": "lowered into a string — the face holds these bytes inside a literal",
    "86:4:span": "lowered into a string — the face holds these bytes inside a literal",
    "92:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "94:8:span": "lowered into a string — the face holds these bytes inside a literal",
    "94:13:key": "render DSL attribute — lowers into a string",
    "98:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "107:12:stash": "rip-native channel/pull spelling — the lowering leaves no tokenizable identifier at this position",
    "107:18:stats": "lowered into a string — the face holds these bytes inside a literal",
    "108:12:stash": "rip-native channel/pull spelling — the lowering leaves no tokenizable identifier at this position",
    "108:18:flags": "lowered into a string — the face holds these bytes inside a literal",
    "109:10:stash": "rip-native channel/pull spelling — the lowering leaves no tokenizable identifier at this position",
    "109:16:rows": "lowered into a string — the face holds these bytes inside a literal",
    "109:28:id": "member read on `params`, which hovers `any` (verified 2026-08-03) — no symbol, no token; tsgo behaves identically on hand-written TS",
    "110:29:stash": "rip-native channel/pull spelling — the lowering leaves no tokenizable identifier at this position",
    "110:35:user": "lowered into a string — the face holds these bytes inside a literal",
    "111:19:stash": "rip-native channel/pull spelling — the lowering leaves no tokenizable identifier at this position",
    "111:25:mode": "lowered into a string — the face holds these bytes inside a literal",
    "112:20:stash": "rip-native channel/pull spelling — the lowering leaves no tokenizable identifier at this position",
    "112:26:entries": "lowered into a string — the face holds these bytes inside a literal",
    "112:41:tab": "member read on `@query`, which hovers `any` (verified 2026-08-03) — no symbol, no token; tsgo behaves identically on hand-written TS",
    "115:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "117:6:span": "lowered into a string — the face holds these bytes inside a literal",
    "123:2:offer": "gate spelling — lowers to gate scaffold, not an identifier",
    "126:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "129:2:accept": "gate spelling — lowers to gate scaffold, not an identifier",
    "132:4:span": "lowered into a string — the face holds these bytes inside a literal",
    "140:4:svg": "lowered into a string — the face holds these bytes inside a literal",
    "140:8:viewBox": "lowered into a string — the face holds these bytes inside a literal",
    "140:53:fill": "lowered into a string — the face holds these bytes inside a literal",
    "142:8:circle": "lowered into a string — the face holds these bytes inside a literal",
    "142:15:cx": "lowered into a string — the face holds these bytes inside a literal",
    "142:25:cy": "lowered into a string — the face holds these bytes inside a literal",
    "143:6:path": "lowered into a string — the face holds these bytes inside a literal",
    "163:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "164:6:aria": "lowered into a string — the face holds these bytes inside a literal",
    "164:11:busy": "lowered into a string — the face holds these bytes inside a literal",
    "165:6:data": "lowered into a string — the face holds these bytes inside a literal",
    "165:11:kind": "lowered into a string — the face holds these bytes inside a literal",
    "187:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "188:6:ref": "component `ref` prop — lowers to `this`-rooted wiring; the face holds no such identifier at this position",
    "195:25:button": "lowered into a string — the face holds these bytes inside a literal",
    "200:4:button": "lowered into a string — the face holds these bytes inside a literal",
    "202:6:slot": "render DSL word — lowers into a string",
    "211:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "218:8:div": "lowered into a string — the face holds these bytes inside a literal",
    "219:25:click": "lowered into a string — the face holds these bytes inside a literal",
    "220:16:click": "lowered into a string — the face holds these bytes inside a literal",
    "218:12:row": "render DSL class shorthand — lowers into the class string",
    "227:2:accept": "gate spelling — lowers to gate scaffold, not an identifier",
    "230:4:span": "lowered into a string — the face holds these bytes inside a literal",
    "227:21:Far": "namespace head of a provider path, read through `typeof` — tsgo leaves it untokenized on equivalent hand-written TS — probe-verified 2026-09-26"
  },
  "14-schema.rip": {
    "30:12:datetime": "lowered into a string — the face holds these bytes inside a literal",
    "41:21:Id": "member read on `it`, which hovers `any` (verified 2026-08-03) — no symbol, no token; tsgo behaves identically on hand-written TS",
    "42:21:DisplayName": "member read on `it`, which hovers `any` (verified 2026-08-03) — no symbol, no token; tsgo behaves identically on hand-written TS",
    "50:15:shape": "symbol literal — lowers into a string",
    "56:3:ensure": "lowered into a string — the face holds these bytes inside a literal",
    "56:33:items": "symbol literal — lowers into a string",
    "67:3:open": "symbol literal — lowers into a string",
    "68:3:closed": "symbol literal — lowers into a string",
    "82:18:union": "symbol literal — lowers into a string",
    "83:7:kind": "symbol literal — lowers into a string",
    "95:18:mixin": "symbol literal — lowers into a string",
    "99:3:mixin": "lowered into a string — the face holds these bytes inside a literal",
    "109:18:model": "symbol literal — lowers into a string",
    "111:13:email": "symbol literal — lowers into a string"
  },
  "20-inference.rip": {
    "67:8:toUpperCase": "member read on `media`, which hovers `any` (verified 2026-08-03) — no symbol, no token; tsgo behaves identically on hand-written TS"
  },
  "22-vocabulary.rip": {
    "26:60:alpha": "member resolves through `Tallies`'s index signature — no declared property symbol, no token; probe-verified identical on hand-written TS"
  },
  "25-components.rip": {
    "25:27:input": "lowered into a string — the face holds these bytes inside a literal",
    "29:4:input": "lowered into a string — the face holds these bytes inside a literal",
    "37:4:span": "lowered into a string — the face holds these bytes inside a literal",
    "45:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "49:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "61:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "62:6:input": "lowered into a string — the face holds these bytes inside a literal",
    "62:12:ref": "component `ref` prop — lowers to `this`-rooted wiring; the face holds no such identifier at this position",
    "63:6:section": "lowered into a string — the face holds these bytes inside a literal",
    "63:14:ref": "component `ref` prop — lowers to `this`-rooted wiring; the face holds no such identifier at this position",
    "71:4:input": "lowered into a string — the face holds these bytes inside a literal",
    "71:10:ref": "component `ref` prop — lowers to `this`-rooted wiring; the face holds no such identifier at this position",
    "77:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "78:13:inner": "channel spelling (`<=>`) — rip-native; the lowering leaves no tokenizable identifier at this position",
    "131:4:span": "lowered into a string — the face holds these bytes inside a literal",
    "135:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "143:2:offer": "context-channel word — lowers to a setContext/getContext call, not an identifier",
    "146:4:slot": "render DSL word — lowers into a string",
    "149:2:accept": "context-channel word — lowers to a setContext/getContext call, not an identifier",
    "154:4:span": "lowered into a string — the face holds these bytes inside a literal"
  },
  "26-schema.rip": {
    "23:6:datetime": "lowered into a string — the face holds these bytes inside a literal",
    "33:17:shape": "symbol literal — lowers into a string"
  },
  "27-components-lib.rip": {
    "8:4:span": "lowered into a string — the face holds these bytes inside a literal",
    "10:6:slot": "render DSL word — lowers into a string"
  },
  "27-components-use.rip": {
    "9:4:section": "lowered into a string — the face holds these bytes inside a literal"
  }
}
