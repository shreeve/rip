{
  "01-basics.rip": {
    "46:10:alpha": "symbol literal — lowers into a string"
  },
  "03-collections.rip": {
    "9:12:alpha": "lowered into a string — the face holds these bytes inside a literal",
    "9:22:beta": "lowered into a string — the face holds these bytes inside a literal"
  },
  "04-assignments.rip": {
    "83:54:retries": "member read on `metaRest`, which hovers `any` (verified 2026-08-03) — no symbol, no token; tsgo behaves identically on hand-written TS",
    "103:20:alpha": "INTERIM — hoist cost, not twin-equivalent: idiomatic hand-written TS tokens this. `keyName`'s hoist widens it from 'alpha' to string, degrading `dynamic`'s computed key to an index signature — the member read loses its symbol (probe-verified: a const key tokens)"
  },
  "07-exceptions.rip": {
    "22:40:length": "member read on `message`, which hovers `any` (verified 2026-08-03) — no symbol, no token; tsgo behaves identically on hand-written TS"
  },
  "08-functions.rip": {
    "147:33:json": "INTERIM — hoist cost, not twin-equivalent: idiomatic hand-written TS tokens this. `FORMATS` is a hoisted evolving `let`; inside a closure the classifier does not flow-type the capture, so the member carries no token (probe-verified on the face's exact shape)",
    "147:51:text": "INTERIM — hoist cost, not twin-equivalent: idiomatic hand-written TS tokens this. `FORMATS` is a hoisted evolving `let`; inside a closure the classifier does not flow-type the capture, so the member carries no token (probe-verified on the face's exact shape)"
  },
  "10-modules.rip": {
    "14:24:theme": "import-binding head tsgo leaves untokenized on equivalent hand-written TS (namespace head, or a default that exports a literal) — probe-verified 2026-08-03",
    "14:37:lib": "import-binding head tsgo leaves untokenized on equivalent hand-written TS (namespace head, or a default that exports a literal) — probe-verified 2026-08-03",
    "14:71:sameTheme": "import-binding head tsgo leaves untokenized on equivalent hand-written TS (namespace head, or a default that exports a literal) — probe-verified 2026-08-03",
    "22:25:combo": "import-binding head tsgo leaves untokenized on equivalent hand-written TS (namespace head, or a default that exports a literal) — probe-verified 2026-08-03",
    "22:38:whole": "import-binding head tsgo leaves untokenized on equivalent hand-written TS (namespace head, or a default that exports a literal) — probe-verified 2026-08-03",
    "22:50:paired": "import-binding head tsgo leaves untokenized on equivalent hand-written TS (namespace head, or a default that exports a literal) — probe-verified 2026-08-03",
    "32:14:themeShape": "type-only default binding read through `typeof` — tsgo leaves it untokenized on equivalent hand-written TS (a default that exports a literal) — probe-verified 2026-08-31",
    "33:7:libShapes": "type-only namespace head in a qualified type name — tsgo leaves it untokenized on equivalent hand-written TS — probe-verified 2026-08-31",
    "54:23:hubTheme": "import-binding head tsgo leaves untokenized on equivalent hand-written TS (namespace head, or a default that exports a literal) — probe-verified 2026-08-03"
  },
  "11-types.rip": {
    "174:74:hits": "member resolves through `Counts`'s index signature — no declared property symbol, no token; probe-verified identical on hand-written TS",
    "190:14:first": "labeled tuple element — a label, not an identifier with a symbol",
    "190:29:second": "labeled tuple element — a label, not an identifier with a symbol",
    "198:62:length": "tuple `.length` — resolves through the array apparatus, no own symbol; probe-verified: hand-written TS gets no token either"
  },
  "13-components.rip": {
    "13:4:span": "lowered into a string — the face holds these bytes inside a literal",
    "13:33:style": "lowered into a string — the face holds these bytes inside a literal",
    "18:26:input": "lowered into a string — the face holds these bytes inside a literal",
    "24:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "26:8:label": "lowered into a string — the face holds these bytes inside a literal",
    "27:6:input": "lowered into a string — the face holds these bytes inside a literal",
    "28:8:value": "channel spelling (`<=>`) — rip-native; the lowering leaves no tokenizable identifier at this position",
    "30:8:div": "lowered into a string — the face holds these bytes inside a literal",
    "45:4:section": "lowered into a string — the face holds these bytes inside a literal",
    "46:6:h1": "lowered into a string — the face holds these bytes inside a literal",
    "47:6:span": "lowered into a string — the face holds these bytes inside a literal",
    "48:6:input": "lowered into a string — the face holds these bytes inside a literal",
    "48:12:ref": "component `ref` prop — lowers to `this`-rooted wiring; the face holds no such identifier at this position",
    "49:8:value": "channel spelling (`<=>`) — rip-native; the lowering leaves no tokenizable identifier at this position",
    "50:6:button": "lowered into a string — the face holds these bytes inside a literal",
    "50:14:click": "lowered into a string — the face holds these bytes inside a literal",
    "51:6:button": "lowered into a string — the face holds these bytes inside a literal",
    "51:14:click": "lowered into a string — the face holds these bytes inside a literal",
    "55:25:span": "lowered into a string — the face holds these bytes inside a literal",
    "56:27:span": "lowered into a string — the face holds these bytes inside a literal",
    "57:13:span": "lowered into a string — the face holds these bytes inside a literal",
    "58:6:ul": "lowered into a string — the face holds these bytes inside a literal",
    "60:10:li": "lowered into a string — the face holds these bytes inside a literal",
    "60:13:key": "render DSL attribute — lowers into a string",
    "61:6:slot": "render DSL word — lowers into a string",
    "70:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "73:8:value": "channel spelling (`<=>`) — rip-native; the lowering leaves no tokenizable identifier at this position",
    "76:8:em": "lowered into a string — the face holds these bytes inside a literal",
    "84:4:span": "lowered into a string — the face holds these bytes inside a literal",
    "90:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "92:8:span": "lowered into a string — the face holds these bytes inside a literal",
    "92:13:key": "render DSL attribute — lowers into a string",
    "96:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "105:12:stash": "rip-native channel/pull spelling — the lowering leaves no tokenizable identifier at this position",
    "105:18:stats": "lowered into a string — the face holds these bytes inside a literal",
    "106:12:stash": "rip-native channel/pull spelling — the lowering leaves no tokenizable identifier at this position",
    "106:18:flags": "lowered into a string — the face holds these bytes inside a literal",
    "107:10:stash": "rip-native channel/pull spelling — the lowering leaves no tokenizable identifier at this position",
    "107:16:rows": "lowered into a string — the face holds these bytes inside a literal",
    "107:28:id": "member read on `params`, which hovers `any` (verified 2026-08-03) — no symbol, no token; tsgo behaves identically on hand-written TS",
    "108:29:stash": "rip-native channel/pull spelling — the lowering leaves no tokenizable identifier at this position",
    "108:35:user": "lowered into a string — the face holds these bytes inside a literal",
    "109:19:stash": "rip-native channel/pull spelling — the lowering leaves no tokenizable identifier at this position",
    "109:25:mode": "lowered into a string — the face holds these bytes inside a literal",
    "110:20:stash": "rip-native channel/pull spelling — the lowering leaves no tokenizable identifier at this position",
    "110:26:entries": "lowered into a string — the face holds these bytes inside a literal",
    "110:41:tab": "member read on `@query`, which hovers `any` (verified 2026-08-03) — no symbol, no token; tsgo behaves identically on hand-written TS",
    "113:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "115:6:span": "lowered into a string — the face holds these bytes inside a literal",
    "121:2:offer": "gate spelling — lowers to gate scaffold, not an identifier",
    "124:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "127:2:accept": "gate spelling — lowers to gate scaffold, not an identifier",
    "130:4:span": "lowered into a string — the face holds these bytes inside a literal",
    "138:4:svg": "lowered into a string — the face holds these bytes inside a literal",
    "138:8:viewBox": "lowered into a string — the face holds these bytes inside a literal",
    "138:53:fill": "lowered into a string — the face holds these bytes inside a literal",
    "140:8:circle": "lowered into a string — the face holds these bytes inside a literal",
    "140:15:cx": "lowered into a string — the face holds these bytes inside a literal",
    "140:25:cy": "lowered into a string — the face holds these bytes inside a literal",
    "141:6:path": "lowered into a string — the face holds these bytes inside a literal",
    "161:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "162:6:aria": "lowered into a string — the face holds these bytes inside a literal",
    "162:11:busy": "lowered into a string — the face holds these bytes inside a literal",
    "163:6:data": "lowered into a string — the face holds these bytes inside a literal",
    "163:11:kind": "lowered into a string — the face holds these bytes inside a literal",
    "185:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "186:6:ref": "component `ref` prop — lowers to `this`-rooted wiring; the face holds no such identifier at this position",
    "193:25:button": "lowered into a string — the face holds these bytes inside a literal",
    "198:4:button": "lowered into a string — the face holds these bytes inside a literal",
    "200:6:slot": "render DSL word — lowers into a string",
    "209:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "216:8:div": "lowered into a string — the face holds these bytes inside a literal",
    "217:25:click": "lowered into a string — the face holds these bytes inside a literal",
    "218:16:click": "lowered into a string — the face holds these bytes inside a literal",
    "216:12:row": "render DSL class shorthand — lowers into the class string"
  },
  "14-schema.rip": {
    "30:12:datetime": "lowered into a string — the face holds these bytes inside a literal",
    "41:21:Id": "member read on `it`, which hovers `any` (verified 2026-08-03) — no symbol, no token; tsgo behaves identically on hand-written TS",
    "42:21:DisplayName": "member read on `it`, which hovers `any` (verified 2026-08-03) — no symbol, no token; tsgo behaves identically on hand-written TS",
    "50:15:shape": "symbol literal — lowers into a string",
    "56:3:ensure": "lowered into a string — the face holds these bytes inside a literal",
    "56:33:items": "symbol literal — lowers into a string",
    "67:3:open": "symbol literal — lowers into a string",
    "68:3:closed": "symbol literal — lowers into a string",
    "82:18:union": "symbol literal — lowers into a string",
    "83:7:kind": "symbol literal — lowers into a string",
    "95:18:mixin": "symbol literal — lowers into a string",
    "99:3:mixin": "lowered into a string — the face holds these bytes inside a literal",
    "109:18:model": "symbol literal — lowers into a string",
    "111:13:email": "symbol literal — lowers into a string"
  },
  "20-inference.rip": {
    "67:8:toUpperCase": "member read on `media`, which hovers `any` (verified 2026-08-03) — no symbol, no token; tsgo behaves identically on hand-written TS"
  },
  "22-vocabulary.rip": {
    "26:60:alpha": "member resolves through `Tallies`'s index signature — no declared property symbol, no token; probe-verified identical on hand-written TS"
  },
  "25-components.rip": {
    "25:27:input": "lowered into a string — the face holds these bytes inside a literal",
    "29:4:input": "lowered into a string — the face holds these bytes inside a literal",
    "37:4:span": "lowered into a string — the face holds these bytes inside a literal",
    "45:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "49:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "61:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "62:6:input": "lowered into a string — the face holds these bytes inside a literal",
    "62:12:ref": "component `ref` prop — lowers to `this`-rooted wiring; the face holds no such identifier at this position",
    "63:6:section": "lowered into a string — the face holds these bytes inside a literal",
    "63:14:ref": "component `ref` prop — lowers to `this`-rooted wiring; the face holds no such identifier at this position",
    "71:4:input": "lowered into a string — the face holds these bytes inside a literal",
    "71:10:ref": "component `ref` prop — lowers to `this`-rooted wiring; the face holds no such identifier at this position",
    "77:4:div": "lowered into a string — the face holds these bytes inside a literal",
    "78:13:inner": "channel spelling (`<=>`) — rip-native; the lowering leaves no tokenizable identifier at this position"
  },
  "26-schema.rip": {
    "23:6:datetime": "lowered into a string — the face holds these bytes inside a literal",
    "33:17:shape": "symbol literal — lowers into a string"
  },
  "27-components-lib.rip": {
    "8:4:span": "lowered into a string — the face holds these bytes inside a literal",
    "10:6:slot": "render DSL word — lowers into a string"
  },
  "27-components-use.rip": {
    "9:4:section": "lowered into a string — the face holds these bytes inside a literal"
  }
}
