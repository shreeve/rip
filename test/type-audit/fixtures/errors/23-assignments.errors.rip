# 23-assignments.errors.rip — intentional negatives: the Diagnostics Lane
# (bun run type-audit --errors) asserts each one; @ts-nocheck quiets editors only.
# @ts-nocheck

text = 'abc'
port: number = 3000
point = { x: 1 }
box = { size: 1 }
text -= 1
port = 'high'
text .= missing()
*>box = { size: 'big' }

wrongMaybe?: number = 'nine'
{ absent } = point
wrongPick = point.{ absent2: got }
