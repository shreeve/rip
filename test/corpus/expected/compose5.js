let café, t1;

function shout(msg) {
  return `${msg}!`;
}
let who = "crowd";
let m1 = `Hello ${who}`.length;
let c1 = shout(`hey ${who}`);
let v1 = `${who} says ${`hi ${who}`}`;
let ix = `abc${who}`[0];
let rx = /abc/.source;
let n1 = `${(1 + 2)}`;
if (who) {
  t1 = `  inside ${who}`;
}
while (who && (who.length > 99)) {
  who = "";
}
`statement ${who}`;
café = shout(`naïve ${who}`);