let user = {id: 7, name: "ada", role: "admin"};
let basic = ({id: user.id, name: user.name});
let renamed = ({label: user.name});
let defaulted = ({email: (user.email ?? "none")});
let both = ({who: (user.name ?? "anon"), id: user.id});
let missing = null;
let optHit = (user == null ? undefined : {id: user.id});
let optMiss = (missing == null ? undefined : {id: missing.id});
let calls = [];
let fetch = function() {
  return (calls.push(1), {a: 1, b: 2});
};
let fromCall = ((_) => ({a: _.a, b: _.b}))(fetch());
let fromCallOpt = ((_) => _ == null ? undefined : ({a: _.a}))(fetch());
let box = {inner: {v: 3, w: 4}};
let fromMember = ((_) => ({v: _.v}))(box.inner);
let table = [{k: 9}];
let fromIndex = ((_) => ({k: _.k}))(table[0]);
let either = ((_) => ({role: _.role}))((user || missing));
let guarded = ({role: (user.role ?? ("guest" || "none"))});
let firstKey = ({id: user.id, name: user.name}).id;
let keyCount = Object.keys({id: user.id, name: user.name}).length;
let wide = ({id: user.id, tag: user.name, role: (user.role ?? "basic")});
let trailing = ({id: user.id});
let spaced = ({id: user.id, name: user.name});
let merged = {...({id: user.id, name: user.name}), extra: 1};
let optSpread = {...(missing == null ? undefined : {id: missing.id}), tag: 2};
let callSpread = {...((_) => ({a: _.a}))(fetch()), b: 9};
let deepSpread = {...({id: user.id, role: user.role}), last: true};